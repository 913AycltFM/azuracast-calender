name: Update AzuraCast iCal

on:
  schedule:
    # Runs every 15 minutes (GitHub cron syntax)
    - cron: '*/15 * * * *'
  workflow_dispatch:

jobs:
# ... (the rest of your file remains exactly the same)
